rtspAddress:  :8554
protocols:    [udp, multicast, tcp]

# Disable các service không dùng (tránh xung đột port trên server chia sẻ).
# Mock camera chỉ cần RTSP + control API.
rtmp:         no
hls:          no
webrtc:       no
srt:          no

api:          yes
apiAddress:   :19997
logLevel:     info

paths:
  main:
    # profile_main declare 3840x2160@30 trong Media1/Media2 VEC → serve đúng 4K
    # để RTSS-1-1-42/48 (H.264 RESOLUTION verify) không mismatch.
    runOnInit: >-
      ffmpeg -re -f lavfi -i testsrc2=size=3840x2160:rate=30
      -c:v libx264 -preset ultrafast -tune zerolatency
      -f rtsp -rtsp_transport tcp
      rtsp://127.0.0.1:8554/main
    runOnInitRestart: yes

  jpeg:
    # profile_jpeg = MJPEG 1920x1080@30. RTSS-1-1-31..36/53 cần JPEG stream thực.
    runOnInit: >-
      ffmpeg -re -f lavfi -i testsrc2=size=1920x1080:rate=30
      -c:v mjpeg -q:v 5
      -f rtsp -rtsp_transport tcp
      rtsp://127.0.0.1:8554/jpeg
    runOnInitRestart: yes

  sub1:
    runOnInit: >-
      ffmpeg -re -f lavfi -i testsrc2=size=1280x720:rate=15
      -c:v libx264 -preset ultrafast -tune zerolatency
      -f rtsp -rtsp_transport tcp
      rtsp://127.0.0.1:8554/sub1
    runOnInitRestart: yes

  sub2:
    runOnInit: >-
      ffmpeg -re -f lavfi -i testsrc2=size=640x480:rate=10
      -c:v libx264 -preset ultrafast -tune zerolatency
      -f rtsp -rtsp_transport tcp
      rtsp://127.0.0.1:8554/sub2
    runOnInitRestart: yes
